package com.propswift.Launchers

/*
@ModelView(autoLayout = ModelView.Size.MATCH_WIDTH_WRAP_HEIGHT)
class WelcomeOneModalClass @JvmOverloads constructor(context: Context, attributeSet: AttributeSet? = null, defStyle: Int = 0) : LinearLayout(context, attributeSet, defStyle) {

    private var binding: LauncherWelcomeoneTextBinding

    init {
        binding = LauncherWelcomeoneTextBinding.inflate(LayoutInflater.from(context))
        addView(binding.root)
    }

    @CallbackProp fun setItemClickListener(maleButtonListener: OnClickListener?) {
        binding.amaman.setOnClickListener {
            context.makeLongToast("Male Clicked")
            gender = "Male"
        }
        binding.amawoman.setOnClickListener {
            context.makeLongToast("Female Clicked")
            gender = "Female"
        }
        binding.amanother.setOnClickListener {
            context.makeLongToast("Female Clicked")
            gender = "Other"
        }
    }

}*/
